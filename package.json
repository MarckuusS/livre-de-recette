{
  "name": "livre-de-recettes",
  "version": "0.1.0",
  "private": true,
  "description": "Livre de recettes numerique — webapp PWA (Cloudflare Pages + Workers + D1 + R2)",
  "type": "module",
  "workspaces": [
    "shared",
    "worker",
    "web"
  ],
  "scripts": {
    "typecheck": "tsc -p shared/tsconfig.json && tsc -p worker/tsconfig.json && tsc -p web/tsconfig.json",
    "test": "npm run test --workspaces --if-present",
    "lint": "eslint .",
    "dev:worker": "npm --workspace worker run dev",
    "dev:web": "npm --workspace web run dev",
    "db:migrate:local": "wrangler d1 migrations apply livre-de-recettes --local",
    "db:migrate:remote": "wrangler d1 migrations apply livre-de-recettes --remote",
    "// smoke": "Eprouve l'API d'ecriture contre le Worker LOCAL. Voir l'en-tete du script : il exige SMOKE_USER / SMOKE_PASSWORD et refuse toute cible autre que localhost.",
    "smoke": "node scripts/smoke-api.mjs",
    "// smoke:courses": "Eprouve la session de courses (chariot, correspondance, validation en cinq etapes). Memes garde-fous que `smoke`.",
    "smoke:courses": "node scripts/smoke-courses.mjs",
    "// smoke:isolation": "PROUVE que deux cuisines ne se voient pas. Exige deux comptes dans des foyers differents (voir --cuisine= dans add-user.mjs) et SMOKE_A_* / SMOKE_B_*.",
    "smoke:isolation": "node scripts/smoke-isolation.mjs",
    "db:check": "node scripts/check-fts5.mjs",
    "db:export": "node scripts/migrate-sqlite-to-d1.mjs",
    "db:verify": "node scripts/migrate-sqlite-to-d1.mjs && node scripts/verify-migration.mjs",
    "db:load:local": "wrangler d1 execute livre-de-recettes --local --file=scripts/_dump/d1-seed.sql",
    "db:load:remote": "wrangler d1 execute livre-de-recettes --remote --file=scripts/_dump/d1-seed.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250109.0",
    "@types/node": "^22.10.7",
    "esbuild": "^0.24.2",
    "typescript": "^5.7.3",
    "vitest": "^3.0.5",
    "wrangler": "^3.105.0"
  },
  "engines": {
    "node": ">=20"
  }
}
